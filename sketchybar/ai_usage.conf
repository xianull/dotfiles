# SketchyBar AI 订阅用量 —— 显示哪些、按什么顺序显示
#
# 每行一个 provider id，从上到下 = 栏内从左到右。
# 行首加 # 即可隐藏该项；改完执行 sketchybar --reload 生效。
#
# 当前支持：
#   cursor   Cursor（token 取自 Cursor 的 globalStorage，走 api2.cursor.sh）
#   grok     Grok（token 取自 ~/.grok/auth.json，走 cli-chat-proxy.grok.com）

cursor
grok
